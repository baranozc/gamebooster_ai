@model List<GameBooster.Core.Entities.UserSystem>

@{
    ViewData["Title"] = "Kullanıcı Sistemleri";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3>💻 @ViewBag.UserName Adlı Kullanıcının Sistemleri</h3>
        <a asp-action="UserList" class="btn btn-secondary">⬅️ Kullanıcı Listesine Dön</a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }

    @if (Model.Count == 0)
    {
        <div class="alert alert-warning text-center">
            Bu kullanıcının kayıtlı hiç sistemi yok.
        </div>
    }
    else
    {
        <div class="card shadow border-secondary">
            <div class="card-body p-0">
                
                <table class="table table-dark table-hover table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Sistem Adı</th>
                            <th>Ekran Kartı</th>
                            <th>İşlemci</th>
                            <th>RAM</th>
                            <th>Oluşturma Tarihi</th>
                            <th class="text-end">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var sys in Model)
                        {
                            <tr>
                                <td class="fw-bold text-info">@sys.SystemName</td>
                                <td>@sys.GPU.Name</td>
                                <td>@sys.CPU.Name</td>
                                <td>@sys.RamAmount GB</td>
                                <td>@sys.CreatedDate.ToShortDateString()</td>
                                <td class="text-end">
                                    <form asp-action="DeleteSystem" method="post" class="d-inline">
                                        <input type="hidden" name="systemId" value="@sys.Id" />
                                        <input type="hidden" name="userId" value="@sys.AppUserId" /> 
                                        
                                        <button type="submit" class="btn btn-sm btn-outline-danger" 
                                                onclick="return confirm('Bu sistemi (Admin yetkisiyle) silmek istediğinize emin misiniz?');">
                                            🗑️ Sil
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
</div>