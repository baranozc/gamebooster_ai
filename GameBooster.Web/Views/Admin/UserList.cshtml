@model IEnumerable<GameBooster.Core.Entities.AppUser>

@{
    ViewData["Title"] = "Kullanıcı Listesi";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>👥 Üye Listesi</h2>
        <a asp-action="Index" class="btn btn-secondary">⬅️ Admin Panele Dön</a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }

    <div class="card shadow">
        <div class="card-body p-0">
            <table class="table table-hover table-striped mb-0">
                <thead class="bg-light">
                    <tr>
                        <th>Ad Soyad</th>
                        <th>Email</th>
                        <th>Kayıt Tarihi</th>
                        <th class="text-end">İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model)
                    {
                        <tr>
                            <td class="align-middle fw-bold">@user.FirstName @user.LastName</td>
                            <td class="align-middle">@user.Email</td>
                            <td class="align-middle">@user.CreatedDate.ToShortDateString()</td>
                            <td class="text-end">
                                <a asp-action="UserSystems" asp-route-id="@user.Id" class="btn btn-sm btn-info text-white me-1">💻 Sistemleri</a>
                                <a asp-action="EditUser" asp-route-id="@user.Id" class="btn btn-sm btn-warning">✏️ Düzenle</a>
                                
                                <form asp-action="DeleteUser" method="post" class="d-inline">
                                    <input type="hidden" name="id" value="@user.Id" />
                                    <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Bu kullanıcıyı silmek istediğine emin misin?');">🗑️ Sil</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            
            @if (!Model.Any())
            {
                <div class="p-4 text-center text-muted">
                    Henüz hiç "Member" rolünde üye yok.
                </div>
            }
        </div>
    </div>
</div>